#!/bin/sh

personal_repo="$HOME/projects"
org_repo="$HOME/oprojects"

o_base="$HOME/.git-cron/or"
p_base="$HOME/.git-cron/pr"

push() {
	update_repos_base
}

update_repos_base() {
	if [ -r "$(which push-star)" ]
	then
		
	fi
}

have git push_star && push
